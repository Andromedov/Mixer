package me.andromedov.mixer.api.gui;

/** Stable identifiers for the decorative and control items in the portable speaker menu. */
public enum PortableSpeakerMenuElement {
    FILLER,
    START,
    DSP,
    STOP,
    PREVIOUS,
    SHUFFLE_DISABLED,
    SHUFFLE_ENABLED,
    REPEAT_OFF,
    REPEAT_ALL,
    REPEAT_ONE,
    PAUSE,
    RESUME,
    NEXT
}
